# Kaggle-specific requirements
# The cached Falcon-7B remote code (modeling_falcon.py) uses get_head_mask,
# which was removed in transformers v5+. Pin to 4.40.x for compatibility.

# Core — pin to known-compatible set
transformers==4.40.2
accelerate==0.29.3
peft==0.10.0
bitsandbytes>=0.43.0

# Evaluation
rouge-score==0.1.2
loralib==0.1.2
